{
  "name": "ts-bug",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "generate-types": "tsc --project tsconfig.json --declaration --declarationMap --emitDeclarationOnly --outdir build",
    "typecheck": "tsc --pretty",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
